#include<stdio.h>
int main(){
    //1) Calulate the average of the three integers. Numbers are given by the user.

    // int a,b,c;
    // float d;
    // printf("Enter three numbers:");
    // scanf("%d%d%d",&a,&b,&c);
    // d = (a+b+c)/3.0;
    // printf("The avg of %d, %d, and %d is %f",a,b,c,d);



    // 2) calculate the circumference 

    // int r;
    // float c;
    // printf("Enter the radius :");
    // scanf("%d",&r);
    // c= 2*3.142*r;
    // printf("the circumference of %d radius is %f",r,c);
    // 


    // 3) WAP to calculate simple interest

    // int p,r,t;
    // float si;
    // printf("enter principle, rate, and time :");
    // scanf("%d%d%d",&p,&r,&t);
    // si = p*r*t/100.0;
    // printf("Simple Interest is %f",si);
    // return 0;


    // 4) area of cuboid l*b*h
    // int l,b,h;
    // float result;
    // printf("Enter the length :");
    // scanf("%d%d%d",&l,&b,&h);

    // result = l*b*h;
    // printf("area of cuboid is : %f",result);

    // 5)WAP to ask user the cost price and selling price banana per dozen. calculate the profit or loss earned upon selling 25  banana.
    // float cp,sp,profit;
    // printf("Enter cost price and selling price :");
    // scanf("%f%f",&cp,&sp);
    // profit = (sp-cp)*25/12;

    // printf("the profit is %f",profit);


    // 6) WAP to input  a character from the user and print its ASCII code

    // char a = 'A';
    // printf("The ASCII code of %c is %d",a,a);

    // 7) WAP to input  a integer from the user and print its ASCII character

    // int a;
    // printf("Enter the code :");
    // scanf("%d",&a);
    // printf("The Ascii character is %c",a);


    // 8) WAP to input three character from the user and display character with thier corresponding ASCII codes

    // char a,b,c;
    // printf("Enter the three character :");
    // scanf("%c %c %c",&a,&b,&c);
    // // scanf("%c,%c,%c",&a,&b,&c);
    // printf("The ASCII codes is %d, %d and %d",a,b,c);


    // 9)
    // int d,m,y;
    // printf("Enter date (dd/mm/yyyy)");
    // scanf("%d/%d/%d",&d,&m,&y);
    // printf("%d/%d/%d",d,m,y);

    // 10) WAP to take an input in below given format and convert the time and display the result as given below 

    // int h,m;
    // printf("Enter time in (HH:MM)");
    // scanf("%d:%d",&h,&m);
    // printf("%d hour and %d minute",h,m);

    return 0;


}