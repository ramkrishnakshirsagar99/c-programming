#include<stdio.h>
int main()
{
    // 1) Write a program to input a three digit number and display the sum of the digits.
    // int a,b,c,result;
    // printf("Enter three number :");
    // scanf("%d %d %d",&a,&b,&c);
    // result=a+b+c;
    // printf("The sum of three number is :%d",result);


    // 2)Write a program to find ASCII code of the character '+.

    // char ch = '+';
    // printf("%d",ch);

    // 3) Write a program to print size of an int, a float, a char and a double type variable 
    
    // int a;
    // float b;
    // char ch;
    // double d;
    //  printf("%d \n",sizeof(a));
    //  printf("%d \n",sizeof(b));
    //  printf("%d \n",sizeof(ch));
    //  printf("%d \n",sizeof(d));
    
    
    
    // 4. Write a program to make the last digit of a number stored in a variable as zero. (Example - if x=2345 then make it x=2340)

    // int a = 2345;
    // printf("%d",a/10*10);

    //5) Write a program to input a number from the user and also input a digit. Append a digit in the number and print the resulting number. (Example-number-234 and digit-9 then the resulting number is 2349

    // int a;;
    // int b;
    // printf("Enter a number ");
    // scanf("%d",&a);
    // printf("Enter a digit which You want to append ");
    // scanf("%d",&b);

    // printf("The number is %d",a*10+b);


    // 6) Assume price of 1 USD is INR 84.23. Write a program to take the amount in INR and convert it into USD

    // int r;
    // float d;
    // printf("Enter a rupees ");
    // scanf("%d",&r);

    // d =  r/84.23;
    // printf("The USD is %0.2f",d);

    // 7) Write a program to take a three digit number from the user and rotate its digits by one position towards the right.

    // int a;
    // printf("enter a three number ");
    // scanf("%d",&a);
    // printf("%d",((a%10)*100)+a/10);

    // 8) 8. What will be the value stored in the variable x after executing following statement: x=10>8>4;

    // int x ;
    // x = 10>8>4;
    // printf(" values is %d",x);

    // 9) 9. What will be the value stored in the variable x after executing following statement:x=!12>-2

    // int x=!2>-2;
    // printf(" values is %d",x);
    
    // 10) What will be the value stored in the variable x after executing following statement: x=3<0&&5>0;

    // int x=3 < 0 && 5 > 0;
    // printf(" values is %d",x);


    
}